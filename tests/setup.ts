// Vitest setup file. Runs once before each test file.
//
// PR 2: Nothing to configure. The app shell does not yet touch IndexedDB.
// PR 3 will add `import 'fake-indexeddb/auto';` here once Dexie lands.

export {};
