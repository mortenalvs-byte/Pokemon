export function renderBrowse(): string {
  return `
    <section class="placeholder">
      <h1>Browse</h1>
      <p>Søk og filter for kortdatabasen. Kommer i PR 6 etter at API-syncen i PR 5 har lagret kort i IndexedDB.</p>
    </section>
  `;
}
