export function renderCollection(): string {
  return `
    <section class="placeholder">
      <h1>Min samling</h1>
      <p>Tabellen over alle holdings (raw og graded) kommer i PR 7.</p>
    </section>
  `;
}
