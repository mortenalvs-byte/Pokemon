export function renderSettings(): string {
  return `
    <section class="placeholder">
      <h1>Innstillinger</h1>
      <p>API-key, defaults, valuta, persistent storage og Danger Zone kommer fra og med PR 5.</p>
    </section>
  `;
}
