export function renderBackup(): string {
  return `
    <section class="placeholder">
      <h1>Backup</h1>
      <p>JSON-eksport, validering og restore med forhåndsvisning kommer i PR 4 (rett etter at Dexie-skjemaet i PR 3 er på plass).</p>
    </section>
  `;
}
